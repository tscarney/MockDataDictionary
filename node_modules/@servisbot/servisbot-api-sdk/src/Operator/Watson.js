/*
  communicate with IBM Watson
  focused on mapping Orchestrator object into Watson
*/
module.exports = function watsonOperatorInjector(logger, Operator) {
  return class WatsonOperator extends Operator {
    constructor(params) {
      super(params);
      this.runtimeName = 'watson';
    }
  };
};
