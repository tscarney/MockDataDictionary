module.exports = function parseSlots(utterance) {
  const slots = utterance.match(/\${[a-zA-Z0-9.]+}/g);
  if (!slots) {
    return [];
  }
  return slots.map((s) => {
    const slot = s.replace(/^\${|}$/g, '');
    return { businessSlot: slot };
  });
};
