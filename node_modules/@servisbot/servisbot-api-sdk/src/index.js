const fetch = require('cross-fetch');
const Promise = require('bluebird');
const shortid = require('shortid');
const Logger = require('@servisbot/sb-logger');
const ServisBotApiSdk = require('./ServisBotApiSdk');

const ServisBotSdk = ServisBotApiSdk(fetch, Promise, Logger, shortid);
module.exports = ServisBotSdk;
